# Single Paste to Generate a Complete Professional Full-Stack System

## PROJECT

**Project Name:** []

**Exam Year:** [2026]

**Root Folder Name:** [Musabende_Michel_National_practical_Exam_2026]

## SCENARIO

[ ]

## OBJECTIVE

Generate a complete, exam-grade full project (frontend + backend + seed + README + test script) that:

- Implements MVC backend (Node.js + Express + Mongoose for MongoDB).
- Implements React 18 + Vite frontend with Tailwind CSS UI.
- Uses Axios for API calls and session-based authentication with bcrypt.
- Assigns **role: "admin"** to every created user by default.
- Prevents infinite re-renders and 401 loops.
- Includes export (SheetJS/jsPDF) for reports.
- Includes `AutoGenerateField` component and `hooks/useAutoGenerate.js`.
- Adds **Logout** in the navbar and shows the **project name in the header**.
- Is fully tested and runnable with a seed script demonstrating default admin behavior.

## REQUIREMENTS SUMMARY

- **Default admin**: User schema must include `role: { type: String, default: "admin" }`. All created users must have `role: "admin"`.
- **Folder layout**: produce files exactly as shown in the ARCHITECTURE section.
- **Auth & stability**:
  - Session-based auth with bcrypt; security question stored hashed for recovery.
  - `AuthProvider` checks session once on app load using a `useRef` guard.
  - Axios `apiClient` uses `withCredentials: true` and interceptors that:
    - Attach cookies/credentials.
    - On 401: show a single toast and redirect to `/login` once; no infinite retry loops.
  - `useEffect` hooks must use correct dependency arrays and guards to avoid infinite re-renders.
- **UI**:
  - Header shows **project name** and greeting `Welcome back, [username]!` with avatar.
  - Sidebar dark theme, full height, collapsible with hamburger toggle, footer pinned to bottom; **Logout** available in header and sidebar.
  - Landing hero with blue→purple gradient and "Get Started" → `/login`.
  - Dashboard with stat cards, quick actions, recent activity; optional mini charts (Recharts allowed).
  - Entity pages with search (debounced), responsive DataTable, Add/Edit modal, Delete confirm dialog, toasts, skeleton loaders.
  - Reports page with date range, summary cards, export (CSV/Excel/PDF), print; exported PDF includes company header, Report ID, Generated By, page numbers.
- **AutoGeneration**: server-side IDs, timestamps, `createdBy`, and calculated fields; include `AutoGenerateField` component and `hooks/useAutoGenerate.js`.
- **Validations**: required fields, email format, password min 6 chars, confirm match, phone numeric and length, date validity, numeric ranges, unique constraints enforced at DB level.
- **Reports & export**: use SheetJS and jsPDF (or equivalent) for CSV/Excel/PDF; include print support and report metadata.
- **Seed & tests**:
  - `backend/seed.js` must create sample data and at least one user: username `admin`, password `Admin@123`.
  - Provide a short test script or instructions that:
    - Starts backend and frontend.
    - Runs a sanity sequence: login (seeded admin), create records, generate a report, export CSV/PDF, logout.
    - Prints a pass/fail summary.
- **Deliverables**: full file contents, README, `.env.example`, run/test instructions, and sample screenshots.

## TECH STACK

| Layer | Technology |
|-------|-----------|
| Frontend | React 18 + Vite |
| Styling | Tailwind CSS |
| Routing | react-router-dom |
| Icons | lucide-react |
| Backend | Node.js + Express.js |
| Database | MongoDB + Mongoose |
| API | Axios |
| Auth | Session-based with bcrypt |
| Exports | SheetJS and jsPDF (or equivalent) |

## ARCHITECTURE AND FOLDER STRUCTURE

Produce files matching this layout and include full file contents in the response:

```
[ROOT_FOLDER]/
├── backend-project/
│   ├── config/
│   │   └── db.js
│   ├── controllers/
│   │   ├── authController.js
│   │   ├── <entity>Controller.js
│   │   └── reportController.js
│   ├── middleware/
│   │   ├── auth.js
│   │   ├── errorHandler.js
│   │   └── validate.js
│   ├── models/
│   │   ├── User.js
│   │   ├── <Entity>.js
│   │   └── <Entity>.js
│   ├── routes/
│   │   ├── authRoutes.js
│   │   ├── <entity>Routes.js
│   │   └── reportRoutes.js
│   ├── utils/
│   │   └── exportUtils.js
│   ├── seed.js
│   ├── .env.example
│   ├── package.json
│   └── server.js
└── frontend-project/
    ├── public/
    ├── src/
    │   ├── assets/
    │   ├── components/
    │   │   ├── Header.jsx
    │   │   ├── Sidebar.jsx
    │   │   ├── Layout.jsx
    │   │   ├── Modal.jsx
    │   │   ├── Toast.jsx
    │   │   ├── ConfirmDialog.jsx
    │   │   ├── AutoGenerateField.jsx
    │   │   └── DataTable.jsx
    │   ├── contexts/
    │   │   └── AuthContext.jsx
    │   ├── hooks/
    │   │   ├── useAutoGenerate.js
    │   │   └── useDebounce.js
    │   ├── pages/
    │   │   ├── Landing.jsx
    │   │   ├── Login.jsx
    │   │   ├── Register.jsx
    │   │   ├── Recovery.jsx
    │   │   ├── Dashboard.jsx
    │   │   ├── <Entity>List.jsx
    │   │   ├── <Entity>Form.jsx
    │   │   └── Reports.jsx
    │   ├── services/
    │   │   ├── apiClient.js
    │   │   ├── authService.js
    │   │   └── <entity>Service.js
    │   ├── App.jsx
    │   ├── main.jsx
    │   ├── index.css
    │   └── App.css
    ├── package.json
    ├── tailwind.config.js
    └── vite.config.js
```

## BACKEND SPECIFICATIONS

- Mongoose schemas include `timestamps: true`, `createdBy`, and `role` default `"admin"`.
- Controllers implement business logic, server-side validations, and calculated fields.
- Routes expose RESTful endpoints for CRUD, auth, reports, and exports.
- Middleware: `auth` attaches `req.user`; `errorHandler` returns `{ success, message, data? }`.
- CORS configured to allow frontend origin and credentials; secure cookie options in production.
- `seed.js` creates sample data and demonstrates default admin assignment.

## FRONTEND SPECIFICATIONS

- `AuthProvider` checks session once on load and exposes `user`, `login`, `logout`, `register`, `recover`.
- `apiClient` uses `withCredentials: true`, baseURL, and interceptors to handle 401 once.
- Layout uses `100vh`, zero gaps, flex layout: dark full-height sidebar + main content; footer pinned to bottom of sidebar.
- Header shows project name and greeting; Logout available in header and sidebar.
- Reusable components and hooks included and documented.

## VALIDATIONS

- Implement frontend and backend validations for required fields, email, password, phone, dates, numeric ranges.
- Enforce unique constraints at DB level and handle conflicts in controllers.

## REPORTS AND EXPORT

- Reports include header/footer, Report ID, Generated By, page numbers.
- Export using SheetJS and jsPDF for CSV/Excel/PDF; include print support.

## ERROR HANDLING AND STABILITY

- Controllers return consistent JSON.
- Frontend shows toast messages for errors and successes.
- Prevent infinite re-render and 401 loops via guarded `useEffect` and Axios interceptors.

## ERD AND SCHEMA

- Generate a text ERD for the pasted scenario showing entities, attributes, PKs, FKs, and cardinalities.
- Provide Mongoose schemas for all entities with indexes and unique constraints where needed.

## SEED AND README

- `backend/seed.js` creates sample data and at least one user (`admin / Admin@123`).
- README includes step-by-step run and test instructions:
  - Backend: `cd backend-project`, `npm install`, copy `.env.example` to `.env`, `npm run seed`, `npm start`.
  - Frontend: `cd frontend-project`, `npm install`, `npm run dev`.
  - How to run the test script and what to expect.

## TEST SCRIPT

Provide a short test script or instructions that:

- Starts backend and frontend.
- Logs in with seeded admin.
- Creates sample records for each entity.
- Generates a report and exports CSV/PDF.
- Logs out.
- Prints a pass/fail summary.

## DELIVERABLES

- Full file contents for every file in the folder structure above using code blocks labeled with file paths.
- Comments explaining key logic (auth flow, default admin assignment, preventing infinite re-renders).
- Simple, readable code suitable for exam marking.

## EXTRA NOTES FOR THE AI

- Enforce **every created user** has `role: "admin"`.
- Implement `AutoGenerateField` and `hooks/useAutoGenerate.js`.
- Include Recharts for optional mini charts only if allowed; otherwise use cards/lists.
- Use SheetJS/jsPDF for export functionality.
- Prevent infinite re-render and 401 loops by implementing robust auth checks and Axios interceptors.
- Produce the entire project in one response. If not possible, provide full file contents grouped by path and a test output summary.

## START STEPS

1. Generate ERD and MongoDB schemas for the pasted scenario.
2. Create backend code (models, controllers, routes, middleware, seed).
3. Create frontend code (components, pages, contexts, services, hooks).
4. Provide README and `.env.example`.
5. Run the test script and include its output summary in the response.
